# 1️⃣ Python 3.9 슬림 이미지 사용
FROM python:3.9-slim

# 2️⃣ 작업 디렉터리 설정
WORKDIR /app

# 3️⃣ 의존성 설치
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# 4️⃣ 애플리케이션 코드 복사
COPY . .

# 5️⃣ 서버 실행 (app.py → run.py로 변경)
CMD ["python", "-u", "run.py"]
